# Cusdis Comments

Local Quartz component for Cusdis comments.

Comments are opt-in per page. Add this frontmatter to a page:

```yaml
comments: true
```

Set `appId` in `quartz.config.yaml` after creating a site in Cusdis.
